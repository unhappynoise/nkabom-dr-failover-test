# Nkabom DR Test - DNS failover config
listen-address=127.0.0.1,10.10.30.10
bind-interfaces
no-resolv
domain-needed
bogus-priv

# App domain - initially points to Primary
address=/app.nkabom.internal/10.10.10.10

# Short TTL so failover propagates fast
local-ttl=5
